{
  "name": "owner-management-unified",
  "version": "1.0.0",
  "description": "Owner Management full-stack application",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd ../webdashboard && npm install && npm run build && cd ../backend && npm install",
    "build": "cd frontend && npm install && npm run build && cd ../webdashboard && npm install && npm run build && cd ../backend && npm install",
    "render-build": "cd frontend && npm install && npm run build && cd ../webdashboard && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
